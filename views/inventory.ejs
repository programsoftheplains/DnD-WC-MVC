<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Inventory</h1>
    <ul>
    <% inventory.forEach( el => { %>
            <li class='itemName' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo  %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <form action="/inventory/createItem" method='POST'>
        <input type="text" placeholder="Enter Item Name" name='itemName'>
        <input type="text" placeholder="Item Quantity" name="itemNumber">
        <input type="submit">
    </form>

    <a href="/characters">Characters</a>
    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>